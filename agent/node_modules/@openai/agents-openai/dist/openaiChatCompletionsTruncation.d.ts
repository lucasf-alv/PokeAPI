import { ModelBehaviorError } from '@openai/agents-core';
export declare const CHAT_COMPLETIONS_EMPTY_TRUNCATION_ERROR = "Chat Completions response terminated with finish_reason='length' but produced no assistant text, tool call, or refusal.";
declare const CHAT_COMPLETIONS_EMPTY_TRUNCATION_ERROR_BRAND: unique symbol;
export type TruncatedEmptyChatCompletionError = ModelBehaviorError & {
    readonly unsafeToReplay: true;
    readonly responseStarted: true;
    readonly [CHAT_COMPLETIONS_EMPTY_TRUNCATION_ERROR_BRAND]: true;
};
export declare function createTruncatedEmptyChatCompletionError(): TruncatedEmptyChatCompletionError;
export declare function isTruncatedEmptyChatCompletionError(error: unknown): error is TruncatedEmptyChatCompletionError;
export type ChatCompletionsOutputState = {
    finishReason: string | null | undefined;
    hasText: boolean;
    hasRefusal: boolean;
    hasAudio: boolean;
    hasReasoning: boolean;
    hasFunctionCall: boolean;
};
export declare function isTruncatedEmptyChatCompletion({ finishReason, hasText, hasRefusal, hasAudio, hasReasoning, hasFunctionCall, }: ChatCompletionsOutputState): boolean;
export {};
