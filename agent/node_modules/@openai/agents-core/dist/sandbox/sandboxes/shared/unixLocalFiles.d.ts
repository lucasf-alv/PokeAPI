/** Controls optional descriptor-relative protection for Unix host file operations. */
export type LocalFileIOProtection = 'auto' | 'required' | 'off';
export declare const preparedFileIO: unique symbol;
type FileRequest = {
    operation: 'read' | 'image' | 'list' | 'exists' | 'directoryExists' | 'create' | 'update' | 'delete';
    path: string;
    destination?: string;
    unlinkPath?: string;
    limit?: number;
    owner?: {
        uid: number;
        gid: number;
    };
};
/** Owns trusted file workers independently of sandbox shell processes. */
export declare class UnixLocalFiles {
    private readonly executable;
    constructor(mode?: LocalFileIOProtection);
    get backend(): 'python' | 'node';
    private readonly active;
    private closed;
    close(): Promise<void>;
    stop(): Promise<void>;
    run(request: FileRequest, options?: {
        input?: string;
        update?: (current: string) => string;
        identity?: {
            uid: number;
            gid: number;
        };
    }): Promise<Buffer>;
}
export {};
