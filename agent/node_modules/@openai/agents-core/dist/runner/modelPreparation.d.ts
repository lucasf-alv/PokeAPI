import { Agent, AgentOutputType } from '../agent';
import type { RunState } from '../runState';
import { AgentArtifacts, ProcessedResponse } from './types';
import type { ToolNameCollisionPolicy } from './runConfig';
export declare function trackDeferredToolRecovery(state: RunState<any, any>, sourceAgent: Agent<any, any>, response: ProcessedResponse<any>): void;
/**
 * Collects tools and handoffs for the current agent so model calls and tracing share the same
 * snapshot of enabled capabilities.
 */
export declare function prepareAgentArtifacts<TContext, TAgent extends Agent<TContext, AgentOutputType>>(state: RunState<TContext, TAgent>, executionAgent?: Agent<TContext, AgentOutputType>, toolNameCollisionPolicy?: ToolNameCollisionPolicy): Promise<AgentArtifacts<TContext>>;
