import type { Agent } from '../agent';
import type * as protocol from '../types/protocol';
/** Returns a durable name only when it identifies one Agent in the run graph. */
export declare function getToolSearchAgentName(agent: Agent<any, any>, agents: Iterable<Agent<any, any>>): string | undefined;
/** Records SDK-owned discovery attribution without changing the provider payload. */
export declare function attributeToolSearchOutput(output: protocol.ToolSearchOutputItem, agentName: string | undefined): protocol.ToolSearchOutputItem;
